{
  "name": "cli-nodejs-bindec",
  "version": "1.0.0",
  "description": "A CLI application that converts binary numbers to decimal numbers (vice versa).",
  "main": "index.js",
  "author": "Kent Louise Tonino",
  "license": "MIT",
  "scripts": {
    "build": "tsc --build",
    "start": "tsc --build && node ./dist/app.js",
    "format": "prettier --write \"./src/**/*.ts\""
  },
  "dependencies": {
    "@types/node": "^20.5.6",
    "prettier": "^3.0.2",
    "typescript": "^5.2.2"
  }
}
